import React from "react";

const PageTitleModule = ({ props }) => {
  // const { overview, pageImage, publishedOn, subtitle, title } = props;

  return <div>This is the Page Title Module</div>;
};

export default PageTitleModule;
